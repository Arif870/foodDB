// core features in js

//  1. variable

const name = "Arif";
let job = "web dev";

//   2. Condition

if (flase) {
} else if (true) {
} else if (true) {
} else {
}

// 3. loop

// for (), while, do while;
for (let i = 0; i <= 20; i++) {}
let number = 10;
while (number <= 20) {
  number++;
}

// 4. Array

let arr = [1, 2, 4, 5, 7, 6];
arr.push();
arr.pup();
arr.shift();
arr.unshift();
arr.length();
// if items is not in array it will return -1
arr.indexOf(2);

// 5. function

function a(a, b) {
  arguments;
  return;
}
a(12, 20);

// 6. object

const obj = {
  name: "Arif",
  id: 21,
};

let a = "name";
obj.name = "onno name";
obj["name"] = "";
obj[a] = console.log(obj.name);
